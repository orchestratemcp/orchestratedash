{
  "name": "orchestratedash",
  "version": "0.1.0",
  "private": true,
  "description": "Telemetry v1 and Agent DOM v2 contract package for OrchestrateDASH.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "verify": "pnpm test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.8.0",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
