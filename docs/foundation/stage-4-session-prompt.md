# Lane F4 — Stage 4: example evals as repository fixtures, and the installed-proof kit

Tier: Sonnet (the evals exist as a scaffold artefact after stage 2; this
lane makes them first-class fixtures in the repo, adds the two the plan
names that a scaffold cannot self-test, and prepares the installed proof
the orchestrator runs). Read `ux-lanes-common.md` first; base is
`codex/agent-foundation` after stages 1–3 merged (the orchestrator gives
the SHA). No Linear id for this stage (workspace limit) — "MAR-886 stage 4".
Plan: `C:\Users\henri\Documents\DASH-agent-foundation-claude-plan.md` "Etapp 4".

Worktree: `C:\Users\henri\AppData\Local\Temp\wt-f4-s1`
Branch: `000henrik/mar-886-evals-proof`. PR targets `master`.

## Ground truth (verify)

- Stage 2 generates `evals/run-evals.mjs` + `evals/cases.json` into every
  scaffold (`agent-kit/template/evals/`), four deterministic checks against
  the agent's own `runOnce` with doubles (read `docs/foundation/stage-2-handoff.md`
  and `agent-kit/template/evals/run-evals.mjs`). `tests/agent-recipe.test.ts`
  drives a real scaffold; `tests/agent-kit.test.ts` spawns the generated
  agent under a real `Supervisor`; `tests/fixtures/retrying-agent.mjs`
  (stage 3) errors once and retries; `tests/fixtures/legacy-agent-kit-template.mjs`
  (stage 1) is the pre-SDK template.
- The broker refuses unknown operations and unauthorised connections with
  typed refusals (`lib/broker/protocol.ts`, `lib/broker/execute.ts`,
  `broker_audit` rows; smoke 7h/7i). Denied-permission behaviour is proven
  in `electron/smoke.ts` proofs 7f–7n on the installed shell.

## Do

1. **The four evals as repository tests** (`tests/agent-evals.test.ts`),
   each against a real generated agent (kit scaffold in a temp dir) under
   the real `Supervisor`, not only against `runOnce` in-process:
   (a) normal input → the agreed digest (item count, grounded citations);
   (b) missing input (empty `sources.json`, unreachable source) → an
   understandable error or abstention in telemetry (`run_failed`/`skipped`
   with a plain `detail`), never a silent empty success;
   (c) tool failure (a source that 500s, then a broker refusal) → found and
   surfaced on the right step in the trace (`status: error`, structured
   error), not hidden;
   (d) denied permission → the agent's `ask()` for an operation it was not
   granted produces a broker refusal AND `broker_audit` shows the refusal
   AND no provider request was made (use the existing fake provider harness
   from `tests/broker-*.test.ts`).
   Deterministic only; test doubles for sources and providers; no paid
   judge. Keep the scaffold's own `evals/` in sync: if a case in
   `evals/cases.json` and the repo test would disagree, make one import the
   other's fixture data.
2. **Proof table.** `docs/foundation/stage-4-proof.md`: a table of every
   stage-1–4 property (SDK shared, recipe validated before write, drift
   detected, traces on the right step, secrets absent, legacy agent runs,
   the four evals) × environment (fixture test / scratch-store shell / real
   provider / Henrik's installed build) with `proven / planned / n/a` and
   the exact command or proof id — filled for the first three columns from
   the handoffs of stages 1–3 and your own runs; the last column is left
   `planned` for the orchestrator.
3. **Installed-proof kit for the orchestrator** (docs, no execution here):
   `docs/foundation/stage-4-installed-walk.md` — a step list for the sample
   journey on the installed build: Add agent → Try a sample → consent →
   run → open Runs → the "Steps and operations" tree → open the digest →
   stop; plus the error case via *Import an agent folder* of a prepared
   fixture project `tests/fixtures/projects/retrying-scout/` (a full
   scaffolded folder built from `retrying-agent.mjs`'s behaviour, manifest
   valid, recipe present) so the installed build shows a failed-then-retried
   step without editing the real store. Build that fixture project with
   the real CLI in a test so it cannot rot (`tests/fixtures/projects/` is
   regenerated by `tests/agent-evals.test.ts` and committed).
4. Do not touch `runner/**`, `lib/**`, `app/**`, `agent-kit/template/dash-agent-sdk.mjs`,
   `electron/**`, `contracts/**`. If a case is impossible without a product
   change, write it as `planned` with the exact gap.

## Verification

Typecheck; `pnpm vitest run tests/agent-evals.test.ts tests/agent-kit.test.ts tests/agent-recipe.test.ts`;
one full `pnpm test` from PowerShell (clear `%TEMP%\dash-mcp-run-*` first).
Evidence class: fixture tests. Stop condition: PR open
(`test(mar-886/4): the four example evals as repository fixtures; installed
proof kit`), green, `docs/foundation/stage-4-handoff.md`. Wait for
`%TEMP%\wt-f4-s1-install.done`.
